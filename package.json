{
  "name": "prompt-rocket",
  "version": "1.0.0",
  "description": "Your AI coding session launches a Minecraft-style voxel Falcon Heavy — every tool call is thrust",
  "license": "MIT",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js && node --check public/app.js && node --check public/voxel.js && node --check public/audio.js && echo 'syntax check passed'"
  }
}
